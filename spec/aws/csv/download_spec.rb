RSpec.describe Aws::Csv::Download do
  it "has a version number" do
    expect(Aws::Csv::Download::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
